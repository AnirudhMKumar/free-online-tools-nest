---
/**
 * Nav — sticky navigation bar.
 * Logo left, category links center, prominent search right.
 * Mobile: compact command-sheet menu.
 */
import { CATEGORIES, SITE } from "../data/tools";
import { useTranslations, getLangFromUrl } from "../i18n/utils";
import { defaultLang } from "../i18n/ui";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const localePrefix = lang === defaultLang ? "" : `/${lang}`;
const pathname = Astro.url.pathname;

const lh = (path: string) => `${localePrefix}${path}`;
const isActive = (href: string) => href === "/" ? pathname === "/" : pathname.startsWith(href);
---

<header
  id="site-nav"
  class="fixed top-0 left-0 right-0 z-50 bg-canvas/90 backdrop-blur-xl border-b border-hairline"
  style="height: 64px;"
>
  <nav class="container-page flex items-center justify-between h-full gap-4" aria-label={t('nav.mainNav')}>
    <a href="/" class="flex min-w-0 items-center gap-2 no-underline group" aria-label={`${SITE.name} ${t('nav.homeLabel')}`}>
      <span class="truncate text-base sm:text-lg font-semibold tracking-tight text-ink" style="letter-spacing: -0.6px;">
        FreeOnlineToolsNest
      </span>
    </a>

    <div class="hidden lg:flex items-center gap-1 min-w-0">
      <a href="/tools/" class:list={["nav-link", isActive("/tools/") && "active"]} aria-current={isActive("/tools/") ? "page" : undefined}>{t('nav.tools')}</a>
      <a href="/blog/" class:list={["nav-link", isActive("/blog/") && "active"]} aria-current={isActive("/blog/") ? "page" : undefined}>{t('nav.blog')}</a>
      {CATEGORIES.map((cat) => (
        <a href={`/categories/${cat.slug}/`} class:list={["nav-link", isActive(`/categories/${cat.slug}/`) && "active"]} aria-current={isActive(`/categories/${cat.slug}/`) ? "page" : undefined}>
          {cat.name}
        </a>
      ))}
    </div>

    <div class="flex items-center gap-2">
      <button
        id="cmd-k-trigger"
        type="button"
        class="nav-search-trigger"
        aria-label={t('nav.search')}
      >
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <circle cx="11" cy="11" r="8"/>
          <path d="m21 21-4.35-4.35"/>
        </svg>
        <span class="hidden md:inline">Search tools</span>
        <kbd class="hidden md:inline-flex" aria-hidden="true">⌘ K</kbd>
      </button>

      <button
        id="theme-toggle"
        type="button"
        class="nav-icon-btn"
        aria-label={t('nav.theme')}
      >
        <svg class="sun-icon hidden" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
        </svg>
        <svg class="moon-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
        </svg>
      </button>

      <button
        id="mobile-menu-btn"
        type="button"
        class="nav-icon-btn lg:hidden"
        aria-label={t('nav.menu')}
        aria-expanded="false"
      >
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
          <line x1="4" y1="6" x2="20" y2="6"/><line x1="4" y1="12" x2="20" y2="12"/><line x1="4" y1="18" x2="20" y2="18"/>
        </svg>
      </button>
    </div>
  </nav>
</header>

<div
  id="mobile-menu"
  class="fixed inset-0 z-40 hidden bg-canvas/95 backdrop-blur-xl pt-20 px-4 pb-6 lg:hidden"
  role="dialog"
  aria-modal="true"
  aria-label={t('nav.mobileNav')}
>
  <div class="mx-auto flex h-full max-w-md flex-col overflow-y-auto rounded-xl border border-hairline bg-canvas p-4 shadow-modal mobile-menu-panel">
    <div class="mb-4 flex items-center justify-between gap-4">
      <div>
        <p class="section-eyebrow mb-1">Navigation</p>
        <p class="text-lg font-semibold text-ink">Find a Tool</p>
      </div>
      <button
        id="mobile-menu-close"
        type="button"
        class="nav-icon-btn"
        aria-label={t('nav.closeMenu')}
      >
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>

    <button type="button" class="mobile-search-entry" data-open-cmdk>
      <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/>
      </svg>
      <span>Search all 77 tools</span>
      <kbd aria-hidden="true">⌘ K</kbd>
    </button>

    <nav class="mt-5 flex flex-col gap-1" aria-label={t('nav.mobileNav')}>
      <a href="/tools/" class:list={["mobile-nav-link", isActive("/tools/") && "active"]}>{t('nav.tools')}</a>
      <a href="/blog/" class:list={["mobile-nav-link", isActive("/blog/") && "active"]}>{t('nav.blog')}</a>
      <p class="mt-4 px-3 text-xs font-mono text-mute">Categories</p>
      {CATEGORIES.map((cat) => (
        <a href={`/categories/${cat.slug}/`} class:list={["mobile-nav-link", isActive(`/categories/${cat.slug}/`) && "active"]}>
          <span class="mr-2" aria-hidden="true">{cat.icon}</span> {cat.name}
        </a>
      ))}
      <hr class="my-4 border-hairline" />
      <a href={lh('/about/')} class="mobile-nav-link">{t('nav.about')}</a>
      <a href={lh('/contact/')} class="mobile-nav-link">{t('nav.contact')}</a>
      <a href={lh('/faq/')} class="mobile-nav-link">{t('nav.faq')}</a>
    </nav>
  </div>
</div>

<div style="height: 64px;" aria-hidden="true"></div>

<style>
  .nav-link {
    display: inline-flex;
    align-items: center;
    padding: 6px 11px;
    font-size: 14px;
    font-weight: 400;
    line-height: 20px;
    color: var(--color-body);
    border-radius: var(--radius-full);
    transition: color 0.15s ease, background-color 0.15s ease;
    text-decoration: none;
    white-space: nowrap;
  }
  .nav-link:hover,
  .nav-link.active {
    color: var(--color-ink);
    background-color: var(--color-canvas-soft-2);
  }
  .nav-search-trigger,
  .nav-icon-btn {
    min-width: 44px;
    height: 44px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-full);
    color: var(--color-body);
    background: transparent;
    transition: color 0.15s ease, background-color 0.15s ease, box-shadow 0.15s ease;
    cursor: pointer;
    touch-action: manipulation;
  }
  .nav-search-trigger {
    gap: 9px;
    padding: 0 13px;
    border: 1px solid var(--color-hairline);
    background: var(--color-canvas);
    box-shadow: var(--shadow-card);
    font-size: 14px;
    font-weight: 500;
  }
  .nav-search-trigger:hover,
  .nav-icon-btn:hover {
    color: var(--color-ink);
    background-color: var(--color-canvas-soft-2);
  }
  @media (min-width: 1024px) {
    #mobile-menu-btn {
      display: none;
    }
  }
  .nav-search-trigger kbd,
  .mobile-search-entry kbd {
    align-items: center;
    height: 22px;
    padding: 0 6px;
    border: 1px solid var(--color-hairline);
    border-radius: var(--radius-sm);
    color: var(--color-mute);
    background: var(--color-canvas-soft-2);
    font-family: var(--font-mono);
    font-size: 11px;
  }
  .mobile-menu-panel {
    overscroll-behavior: contain;
  }
  .mobile-search-entry {
    display: flex;
    align-items: center;
    gap: 10px;
    width: 100%;
    min-height: 46px;
    padding: 0 12px;
    border: 1px solid var(--color-hairline);
    border-radius: var(--radius-lg);
    background: var(--color-canvas-soft);
    color: var(--color-body);
    font-size: 14px;
    text-align: left;
  }
  .mobile-search-entry span {
    min-width: 0;
    flex: 1;
  }
  .mobile-nav-link {
    display: flex;
    align-items: center;
    min-height: 44px;
    padding: 10px 12px;
    font-size: 15px;
    font-weight: 500;
    color: var(--color-ink);
    border-radius: var(--radius-md);
    text-decoration: none;
    transition: background-color 0.15s ease, color 0.15s ease;
  }
  .mobile-nav-link:hover,
  .mobile-nav-link.active {
    background-color: var(--color-canvas-soft-2);
    color: var(--color-link);
  }
  .dark .sun-icon { display: block; }
  .dark .moon-icon { display: none; }
</style>

<script>
  function initTheme() {
    const toggle = document.getElementById("theme-toggle");
    const html = document.documentElement;
    const saved = localStorage.getItem("theme");
    if (saved === "dark" || (!saved && window.matchMedia("(prefers-color-scheme: dark)").matches)) {
      html.classList.add("dark");
    }
    toggle?.addEventListener("click", () => {
      html.classList.toggle("dark");
      localStorage.setItem("theme", html.classList.contains("dark") ? "dark" : "light");
    });
  }

  function initMobileMenu() {
    const btn = document.getElementById("mobile-menu-btn");
    const closeBtn = document.getElementById("mobile-menu-close");
    const menu = document.getElementById("mobile-menu");
    const cmdkButtons = document.querySelectorAll("[data-open-cmdk]");

    function openMenu() {
      menu?.classList.remove("hidden");
      btn?.setAttribute("aria-expanded", "true");
      document.body.style.overflow = "hidden";
    }
    function closeMenu() {
      menu?.classList.add("hidden");
      btn?.setAttribute("aria-expanded", "false");
      document.body.style.overflow = "";
    }
    btn?.addEventListener("click", () => {
      if (menu?.classList.contains("hidden")) openMenu();
      else closeMenu();
    });
    closeBtn?.addEventListener("click", closeMenu);
    cmdkButtons.forEach((button) => {
      button.addEventListener("click", () => {
        closeMenu();
        document.dispatchEvent(new CustomEvent("open-cmdk"));
      });
    });
    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape" && !menu?.classList.contains("hidden")) closeMenu();
    });
  }

  function initCmdK() {
    document.getElementById("cmd-k-trigger")?.addEventListener("click", () => {
      document.dispatchEvent(new CustomEvent("open-cmdk"));
    });
  }

  initTheme();
  initMobileMenu();
  initCmdK();
</script>
