---
import ToolLayout from "../../layouts/ToolLayout.astro";
import TextHumanizer from "../../components/tools/TextHumanizer";
import { getToolBySlug, getCategoryBySlug } from "../../data/tools";

const tool = getToolBySlug("text-humanizer")!;
const category = getCategoryBySlug(tool.categorySlug)!;
---

<ToolLayout
  title={tool.name}
  description={tool.description}
  toolSlug={tool.slug}
  categorySlug={tool.categorySlug}
  categoryName={category.name}
  longDescription={tool.longDescription}
  keywords={tool.keywords}
>
  <TextHumanizer client:load />
</ToolLayout>